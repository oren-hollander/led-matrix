'use strict'

define(['lodash', 'remote-object'], (_, RemoteObject) => {

  describe("RemoteObject property", function () {
    it("should return a value when .get is called", function () {
  //     const prop = RemoteObject({p: 42})
  //     expect(prop.get()).toBe(42)
      expect(true).toBe(true)
    })
  })
  //
  // const ro = RemoteObject({f: RemoteFunction((a, b) => a+ b), p: RemoteProperty(42, p => console.log(p))})
  // const r2 = RemoteObject({f: (a, b) => a+ b, p: 42})
//

})